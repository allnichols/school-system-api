type Teacher {
  id: ID!
  name: String!
  age: Int!
}

input TeacherInput {
  name: String!
  age: Int!
  email: String!
}

type Query {
  getAllTeachers: [Teacher]
  getTeacherById(id: ID!): Teacher
}

type Mutation {
  createTeacher(teacher: TeacherInput!): Teacher
}